<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:mvc="http://www.springframework.org/schema/mvc"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xsi:schemaLocation="
			http://www.springframework.org/schema/beans
			https://www.springframework.org/schema/beans/spring-beans.xsd
			
			http://www.springframework.org/schema/mvc
			https://www.springframework.org/schema/mvc/spring-mvc.xsd
			
			http://www.springframework.org/schema/context
			https://www.springframework.org/schema/context/spring-context.xsd">

	<mvc:annotation-driven/>

	<!-- JSP 파일들을 WEB-INF 폴더 내 views라는 폴더에 작성하도록 지정하는 설정 ▽
	＊WEB-INF 폴더는 브라우저에서 직접 접근할 수 없으므로 이 폴더 안에 만든 JSP 파일은
	반드시 컨트롤러를 이용해야만 접근이 가능함 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>
	
	<!-- resources/로 시작하는 모든 요청은 /resources 폴더 내에 있는 정적리소스 호출 -->
	<mvc:resources location="/resources/" mapping="/resources/**" />

	<!-- org.zerock.controller 경로를 스캔해서 @Service, @Resitory, @Controller,
	@Component가 붙어 있는 클래스가 있다면 그 클래스를 객체 생성 등록(Bean 등록) -->
	<context:component-scan base-package="org.zerock.controller" />
	

</beans>